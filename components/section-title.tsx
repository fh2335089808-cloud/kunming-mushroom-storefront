export function SectionTitle({ eyebrow, title, description, light = false }: { eyebrow:string; title:string; description?:string; light?:boolean }) { return <div className={light ? 'text-white' : ''}><p className={`eyebrow ${light ? '!text-amber-200' : ''}`}>{eyebrow}</p><h2 className="mt-3 font-serif text-3xl leading-tight sm:text-4xl">{title}</h2>{description && <p className={`mt-4 max-w-xl text-sm leading-7 ${light ? 'text-white/75' : 'text-stone-500'}`}>{description}</p>}</div> }
