import Image from "next/image"; import Link from "next/link"; import type {Product} from "@/lib/data";
export function ProductCard({product}:{product:Product}){return <Link href={`/products/${product.id}`} className="group overflow-hidden rounded-2xl bg-white shadow-soft"><div className="relative h-48 overflow-hidden"><Image src={product.image} alt={product.name} fill className="object-cover transition duration-500 group-hover:scale-105"/></div><div className="p-4"><div className="flex justify-between"><h3 className="font-bold">{product.name}</h3><span className="text-xs text-forest-500">{product.origin}</span></div><p className="mt-2 text-xs text-gray-500">{product.spec} · 库存 {product.stock}</p><p className="mt-3 text-lg font-bold text-clay">¥{product.price}<small className="ml-1 text-xs font-normal text-gray-400">/盒</small></p></div></Link>}
