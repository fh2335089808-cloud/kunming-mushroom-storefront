"use client"; import {useState} from "react"; import {AdminLayout} from "../page"; import {products} from "@/lib/data"; import {Button} from "@/components/ui";
export default function AdminProducts(){const [rows,setRows]=useState(products);return <AdminLayout title="商品管理"><div className="mb-5 flex justify-end"><Button onClick={()=>alert("Demo：已打开新增商品表单")}>+ 新增商品</Button></div><div className="overflow-hidden rounded-2xl bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="bg-forest-50 text-gray-500"><tr><th className="p-4">菌种</th><th>采购价</th><th>销售价</th><th>库存</th><th>供应商</th><th>操作</th></tr></thead><tbody>{rows.map(p=><tr className="border-t border-gray-100" key={p.id}><td className="p-4 font-bold">{p.name}<span className="ml-2 text-xs font-normal text-gray-400">{p.origin}</span></td><td>¥{p.purchase}</td><td className="font-semibold text-clay">¥{p.price}</td><td>{p.stock} 盒</td><td>{p.supplier}</td><td><button className="mr-3 text-forest-700" onClick={()=>alert(`编辑 ${p.name}`)}>编辑</button><button className="text-red-500" onClick={()=>setRows(rows.filter(x=>x.id!==p.id))}>删除</button></td></tr>)}</tbody></table></div></AdminLayout>}
